{
  "name": "browser",
  "version": "0.1.0",
  "private": true,
  "description": "A static web browser that runs on GitHub Pages",
  "scripts": {
    "dev": "python -m http.server 3000 --directory .",
    "serve": "python -m http.server 3000 --directory ."
  }
}
